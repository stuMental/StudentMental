<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="io.student.modules.datacenter.dao.CameraroomDao">
    <select id="queryCameraId" resultType="long">
        select camera_id from school_camera_room_info where camera_addr = #{cameraAddr} and room_id = #{roomAddr} and room_addr = #{roomAddr} and docker = #{dockerName}
    </select>
</mapper>