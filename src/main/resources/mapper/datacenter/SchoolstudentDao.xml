<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="io.student.modules.datacenter.dao.SchoolstudentDao">
    <select id="queryId" resultType="SchoolstudentEntity">
        select * from school_student_class_info where student_number = #{studentId} and dept_id = #{studentRoomId}
    </select>

    <select id="save">
        select * from school_student_class_info where student_number = #{studentId} and dept_id = #{studentRoomId}
    </select>

</mapper>